# The Creative's AI Model Deck

An interactive field guide to creative AI models and platforms, with a brief-based route finder and side-by-side card comparison.

This is a static site. `index.html` and `assets/` are ready to deploy on Vercel with the **Other** framework preset, no build command, and the repository root as the output directory.
